name: Generate CommitHash.txt

on:
  push:

jobs:
  generate_commit_hash:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    env:
      SHA: ${{ github.event.pull_request.head.sha }}
    steps:
    #   - run: echo "${SHA:0:4}" >> CommitHash.txt
      - run: echo "${SHA}"
      - run: echo "${SHA:0:4}"
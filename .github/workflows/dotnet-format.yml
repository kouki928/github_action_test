name: dotnet-format

on: 
    pull_request:
        branches: [main]

jobs:
    formatter:
        runs-on: ubuntu-latest

        steps:
            - name: dotnet check
              run: dotnet --version

            - name: 改行コード
              run: od -c Hutzper.Library.FormsMekiki/task/test.py
name: dotnet-format

on: 
    pull_request:
        branches: [main]

jobs:
    formatter:
        runs-on: ubuntu-latest

        steps:
            - name: dotnet check
              run: dotnet --version

            - name: 改行コード1
              run: od -c Hutzper.Library.FormsMekiki/bin/x64/CommitHash.txt

            - name: 改行コード2
              run: od -c Hutzper.Library.FormsMekiki/task/test.py